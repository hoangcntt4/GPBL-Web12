const BASE = "http://localhost:8080/api/v1";

/**
 * @readonly
 *@enum {string}
 */
const Apis = {
  EMPLOYEES_API: `${BASE}/Employees`,
  DEPARTMENTS_API: `${BASE}/Departments`,
};
export default Apis;
